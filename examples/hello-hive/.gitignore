# Generated by running the harness against this example — the starter is just
# package.json + HIVE.md; everything below is produced by `harness --cwd .`
.claude/
node_modules/
src/
package-lock.json
